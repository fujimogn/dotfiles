#!/bin/sh



if which rvm >/dev/null 2>&1 ; then
  [ -s "${HOME}/.rvm/scripts/rvm" ] && source "${HOME}/.rvm/scripts/rvm"
  rvm use system
fi

sudo gem update --system && \
sudo gem install json google-ime-skk


