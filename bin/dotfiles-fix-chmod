#!/bin/sh
#
# $File: ${DOTDIR}/bin/dotfiles-fix-chmod
# $Date: 2011-09-18T05:52:21+0900$
# vim:filetype=zsh:tabstop=2:shiftwidth=2:fdm=marker:


[ -z "$DOTDIR" ] && exit

chmod 755 $DOTDIR/bin

for file in ${DOTDIR}/bin/*;
do
  chmod u+x $file
done
