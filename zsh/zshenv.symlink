#!/usr/bin/env zsh
#
# $File: .zsh/.zshenv
# $Date: 2011-09-07T17:16:16+0900$
# vim:filetype=sh:tabstop=2:shiftwidth=2:fdm=marker:

if [ -f ${HOME}/.dotfilesrc ]; then
  source ${HOME}/.dotfilesrc
  if [ -n ${DOTDIR} ]; then
    export ZDOTDIR=${DOTDIR}/zsh
  fi
fi
